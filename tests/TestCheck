test_channel.hpp and test_Channel_view.hpp are testing all functions of image::ImageChannel

test_image.hpp
Questions: If you save a single channel like "g", is it on purpose that is is only saved in BW?
In line 80, keys is missing.
Problem in test_image.hpp lines 76 and 80: Channel "y" is not saved!!! Instead: "r" is saved, although nobody wants it.
You cannot open "test_save_channel_selection_rgb.jpg".

test_imgproc.hpp
All functions tested expcept for:
void difference(std::map<std::string,ImageChannel> &channels1,
                std::map<std::string,ImageChannel> &channels2,
                ImageChannel &result);
void abs_difference(std::map<std::string,ImageChannel> &channels1,
                    std::map<std::string,ImageChannel> &channels2,
                    ImageChannel &result);

test_lightfield.hpp
Kann keine .h5-Dateien laden?
Lightfield::getImage() wurde bisher nicht getestet, Ergebnisse, die ich produziere, sind fehlerhaft!!!

